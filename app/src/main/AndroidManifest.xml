<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ualr.mxmckee.hospiceprognosis">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_final_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_final_app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForgotPasswordActivity"></activity>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".PrognosisActivity"></activity>
    </application>

</manifest>